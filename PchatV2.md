PChat 通用边讲边画AI 架构文档（路线B：LLM+虚拟黑板状态跟踪）

文档概述

本文档针对PChat通用边讲边画AI产品，明确基于「LLM + 虚拟黑板状态跟踪」（路线B）的核心架构设计，解决“复杂场景布局乱、推理速度慢”两大核心痛点，兼顾产品“通用化”宏大愿景与工程落地可行性。

核心目标：实现「任意领域、分步边讲边画、布局整洁、推理高效」，区别于ChatTutor的垂直数学定位，保持PChat自研DSL的自由度，同时解决落地中的核心卡点。

架构核心原则：LLM负责逻辑、讲解与区域选择，前端负责布局、坐标计算与避让，不将全量画布状态塞给LLM，通过“区域摘要”控制token量，实现高速响应。

1. 整体架构总览

1.1 架构分层（自上而下，共5层）

核心分层：用户交互层 → 黑板状态管理层 → 指令转换层 → LLM推理层 → 渲染执行层

各层职责清晰拆分，避免耦合，重点解决“状态跟踪不慢、布局不乱”两大核心问题，同时保留PChat自研DSL的扩展性和通用化能力。

1.2 架构核心流转图

用户输入 → 交互层采集 → 黑板状态管理层生成「区域摘要」 → 指令转换层生成Prompt → LLM推理层输出（讲解文本+区域选择+绘图逻辑） → 指令转换层转自研DSL → 渲染执行层分步动画播放 → 黑板状态管理层更新区域占用 → 循环直至完成讲解绘图

1.3 与旧架构差异

- 旧架构：LLM直接输出带坐标的DSL → 渲染播放（复杂场景乱、LLM盲画、token膨胀慢）

- 新架构：LLM仅输出“讲解文本+区域选择+绘图逻辑”，坐标计算、布局避让由前端负责（不盲画、token量可控、速度快）

2. 各层详细设计

2.1 用户交互层（最上层，面向用户）

2.1.1 核心功能

- 接收用户输入：问题提问（如“讲解TCP三次握手”）、播放控制（暂停/继续/上一步/下一步）、画布缩放/移动

- 展示输出内容：实时讲解文本（流式）、分步绘图动画、画布状态提示（可选，如“当前可用区域：右侧”）

- 用户操作反馈：拖拽已画元素（轻量交互）、修改绘图步骤（可选）、导出作品（视频/GIF/DSL源码）

2.1.2 关键设计

画布尺寸：默认800×600（可自适应屏幕），避免过大导致布局分散，过小导致元素拥挤。

2.2 黑板状态管理层（核心层，解决“不乱”+“不慢”）

核心职责：维护虚拟黑板的“空间状态”，不传递全量坐标，仅生成“区域摘要”，供LLM参考，同时接收渲染层反馈，更新状态，是路线B的核心核心。

2.2.1 核心数据结构（可直接落地）

// 黑板状态数据结构（精简，token量可控）
const BlackboardState = {
  canvasSize: { width: 800, height: 600 }, // 画布尺寸（固定，不频繁变更）
  occupiedAreas: ["top-left", "middle-left"], // 已占用区域（仅存区域标识，无坐标）
  availableAreas: ["top-right", "middle-right", "bottom-left", "bottom-center", "bottom-right"], // 可用区域
  currentStep: 3, // 当前绘图步骤（用于分步动画）
  elementSummary: [ // 元素摘要（仅存类型+所在区域，不存坐标）
    { id: "el1", type: "rect", area: "top-left", label: "客户端" },
    { id: "el2", type: "arrow", area: "middle-left", label: "连接请求" }
  ]
};

2.2.2 区域划分设计（通用8区域，兼顾自由度与布局可控）

将画布划分为8个固定区域，避免过多区域导致LLM选择混乱，过少区域导致布局拥挤，覆盖所有通用绘图场景（左右对比、上下分层、全局布局）：

1. top-left：左上区域（适合画起始模块、左侧对比内容）

2. top-right：右上区域（适合画右侧对比内容、辅助模块）

3. middle-left：左中区域（适合画核心流程左侧模块）

4. middle-right：右中区域（适合画核心流程右侧模块）

5. middle-center：中中区域（适合画核心模块、全局汇总）

6. bottom-left：左下区域（适合画备注、补充说明）

7. bottom-center：中下区域（适合画总结模块、流程终点）

8. bottom-right：右下区域（适合画辅助说明、图例）

2.2.3 核心逻辑

- 区域占用更新：渲染层每完成一步绘图，反馈元素所在区域，状态层更新occupiedAreas和availableAreas，移除已占用区域，补充可用区域（若元素删除）。

- 区域摘要生成：每次向LLM请求时，仅提取BlackboardState中的occupiedAreas、availableAreas、elementSummary（精简版），生成不超过50token的摘要，避免token膨胀。

- 冲突检测：前端布局时，若LLM选择的区域已被占用，状态层自动提示前端切换至临近可用区域，无需请求LLM，提升速度。

2.3 指令转换层（衔接层，降低LLM与前端耦合）

2.3.1 核心功能

- Prompt生成：接收黑板状态管理层的“区域摘要”，结合用户问题，生成精简Prompt（控制在150token内），引导LLM输出“讲解文本+区域选择+绘图逻辑”，不引导LLM输出坐标。

- LLM输出解析：解析LLM返回的内容，拆分出“讲解文本”“目标区域”“绘图逻辑”（如“画矩形，标注‘服务器’”），剔除无效信息。

- DSL转换：将解析后的“绘图逻辑+目标区域”，转换为PChat自研DSL（由前端计算具体坐标，DSL中仅需包含区域标识，无需坐标）。

2.3.2 核心Prompt模板（可直接使用，不慢、不乱）

用户问题：{userQuestion}
当前黑板状态：已占用区域{occupiedAreas}，可用区域{availableAreas}，已画元素：{elementSummary}
请你完成以下任务：
1. 讲解文本：输出1句简洁讲解（适配当前步骤，不冗余）；
2. 区域选择：从可用区域中选择1个适合的区域，用于绘制当前步骤内容；
3. 绘图逻辑：说明绘制内容（如“矩形，标注‘服务器’”“箭头，连接客户端与服务器”），无需写任何坐标。
要求：讲解文本与绘图逻辑对应，区域选择不与已占用区域冲突，内容简洁，不冗余。

2.3.3 DSL转换规则（适配自研DSL，示例）

假设自研DSL语法为自定义格式，转换逻辑如下（前端计算坐标后补充）：

// LLM输出解析结果
{
  explainText: "接下来我们在右侧画服务器模块，与客户端对应",
  targetArea: "middle-right",
  drawLogic: "矩形，标注‘服务器’，宽200，高100"
}

// 转换为PChat自研DSL（坐标由前端计算，DSL仅含区域标识）
DRAW: rect(area="middle-right", width=200, height=100, text="服务器")
TEXT: 接下来我们在右侧画服务器模块，与客户端对应

2.4 LLM推理层（大脑层，负责逻辑与讲解）

2.4.1 核心功能

- 接收指令转换层的Prompt，理解用户问题、黑板区域状态、已画元素摘要。

- 输出：流式返回“讲解文本+目标区域+绘图逻辑”，每一步输出内容精简（讲解1句，区域1个，绘图逻辑1条），避免多步内容叠加导致token膨胀。

- 不参与任何坐标计算、布局避让，仅负责“逻辑判断”（画什么、放哪里、讲什么）。

2.4.2 性能优化设计（解决“慢”的核心）

- Prompt控制：单轮Prompt不超过150token，避免全量状态注入导致的token爆炸。

- 流式输出：LLM采用流式返回，每一步讲解+绘图逻辑单独返回，前端实时渲染，无需等待完整输出，提升用户感知速度。

- 模型选择：优先选用轻量模型（如DeepSeek-7B、Llama3-8B），复杂场景可切换至能力更强的模型（如GPT-4o-mini），平衡速度与效果。

2.5 渲染执行层（执行层，负责画图与动画）

核心职责：接收指令转换层的自研DSL，计算具体坐标、处理布局避让，实现分步动画播放，是“不乱”的关键执行层。

2.5.1 核心功能

- 坐标计算：根据DSL中的“目标区域”，自动计算元素的具体x/y坐标（结合区域大小、元素尺寸、间距，避免重叠）。

- 布局避让：同一区域内多个元素时，自动计算间距（默认20px），纵向/横向排列；不同区域元素，不跨区域重叠。

- 分步动画：按照步骤顺序，同步播放“讲解文本”与“绘图动画”（如先显示讲解，再逐步绘制元素，匹配人类讲课节奏）。

- 交互响应：处理用户拖拽操作，拖拽后同步更新黑板状态管理层的区域占用（如元素从top-left拖拽至top-right，更新occupiedAreas）。

2.5.2 坐标计算逻辑（示例，可直接落地）

// 以middle-right区域为例，计算矩形元素坐标
function calculateCoord(area, elementType, width, height) {
  const canvas = { width: 800, height: 600 };
  const areaCoord = { // 各区域基准坐标（固定）
    "top-left": { x: 50, y: 50, maxWidth: 300, maxHeight: 200 },
    "middle-right": { x: 450, y: 150, maxWidth: 300, maxHeight: 200 },
    // 其他区域基准坐标...
  };
  // 计算当前区域已存在元素数量，避免重叠
  const occupiedCount = getElementCountInArea(area);
  // 纵向排列，间距20px
  const y = areaCoord[area].y + occupiedCount * (height + 20);
  return {
    x: areaCoord[area].x,
    y: y,
    width: width,
    height: height
  };
}

3. 核心流程详解（以“讲解TCP三次握手”为例）

3.1 流程步骤（完整闭环）

1. 用户输入：“讲解TCP三次握手的过程”，点击开始。

2. 交互层：采集用户问题，传递至黑板状态管理层。

3. 黑板状态管理层：初始化BlackboardState（occupiedAreas为空，availableAreas为8个区域），生成区域摘要（“已占用区域：无，可用区域：所有，已画元素：无”）。

4. 指令转换层：结合用户问题和区域摘要，生成Prompt，发送至LLM推理层。

5. LLM推理层：流式返回第一步内容（讲解：“TCP三次握手首先由客户端向服务器发送连接请求”；目标区域：top-left；绘图逻辑：“矩形，标注‘客户端’”）。

6. 指令转换层：解析LLM输出，转换为自研DSL（DRAW: rect(area="top-left", width=200, height=100, text="客户端")；TEXT: ...）。

7. 渲染执行层：计算top-left区域的具体坐标（x=50,y=50），绘制矩形，同步播放讲解文本。

8. 黑板状态管理层：接收渲染层反馈，更新BlackboardState（occupiedAreas添加top-left，availableAreas移除top-left，elementSummary添加客户端元素）。

9. 重复步骤4-8，直至完成三次握手的全部讲解与绘图（每一步LLM仅选择可用区域，前端计算坐标，避免重叠）。

3.2 关键优化点（流程中的“快”与“不乱”保障）

- 每一步仅传递精简区域摘要，LLM推理速度与普通聊天一致。

- 坐标计算由前端完成，LLM不参与，避免推理耗时。

- 流式输出+分步渲染，用户无需等待完整输出，感知速度快。

- 区域占用实时更新，LLM不会选择已占用区域，避免布局混乱。

4. 核心痛点解决方案汇总

核心痛点

解决方案

实现细节

复杂场景布局乱

黑板状态跟踪+区域划分+前端布局避让

8区域划分，LLM选区域，前端算坐标，实时更新占用状态

推理速度慢

精简Prompt+流式输出+前端承担坐标计算

单轮Prompt≤150token，LLM不参与坐标，流式实时渲染

自研DSL适配性

指令转换层做适配，DSL仅含区域标识

无需修改DSL核心语法，仅新增area参数，前端补充坐标

通用化能力不足

8区域覆盖所有通用场景，LLM负责逻辑，不限制绘图类型

支持流程图、示意图、类比图等，无需绑定具体领域

5. 扩展性设计（适配宏大愿景）

5.1 区域扩展

若需支持更复杂场景（如多区域对比、分层架构），可新增区域（如top-middle、middle-top），无需修改核心架构，仅更新区域划分规则和坐标计算逻辑。

5.2 DSL扩展

自研DSL可新增元素类型（如圆形、折线、文本框、图例），指令转换层和渲染层同步适配即可，LLM无需修改Prompt，仅需新增绘图逻辑描述。

5.3 多模态扩展

后续可新增语音合成、字幕生成功能，接入TTS接口，将讲解文本转为语音，同步配合绘图动画，实现“语音讲解+边画边播”，架构无需大幅调整。

5.4 交互扩展

可新增“步骤编辑”“元素删除/修改”“画布导出”等功能，仅需在交互层和黑板状态管理层新增对应逻辑，不影响核心推理和渲染流程。

6. 工程落地优先级建议

按“先解决核心痛点，再扩展功能”的顺序，快速落地，避免过度设计：

1. 第一阶段（1-2天）：实现黑板状态管理层的区域划分、状态维护，精简Prompt模板。

2. 第二阶段（2-3天）：修改指令转换层，实现LLM输出解析与DSL转换（新增area参数）。

3. 第三阶段（3-4天）：实现渲染执行层的坐标计算、布局避让逻辑，适配分步动画。

4. 第四阶段（1-2天）：测试复杂场景（如TCP、HTTP、微服务架构），优化区域划分和坐标计算，解决边缘案例（如区域元素过多）。

5. 第五阶段：扩展交互功能（拖拽、导出）、多模态功能，适配更通用场景。

7. 总结

本架构基于路线B（LLM + 虚拟黑板状态跟踪），核心是“LLM负责逻辑与区域选择，前端负责布局与执行”，既保留了PChat通用化、自研DSL的优势，又解决了“复杂图乱、推理慢”的落地痛点。

架构设计兼顾“宏大愿景”与“工程落地”，无需推翻现有自研DSL，仅需新增三层核心逻辑（黑板状态管理、指令转换、前端坐标计算），即可快速落地，且具备极强的扩展性，支撑后续通用化场景的持续迭代。
